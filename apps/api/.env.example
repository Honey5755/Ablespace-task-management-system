# Copy to .env — `npm run setup` does this for you if .env is missing.
DATABASE_URL="file:./dev.db"

# Change this in any non-local environment.
JWT_SECRET="dev-only-secret-change-me"
JWT_EXPIRES_IN="30d"

PORT=4000
CORS_ORIGIN="http://localhost:3000"
